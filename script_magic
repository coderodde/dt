TAG=$(cat ~/.dt/633.directory.tagger.tag.633)\n
if [ -z "$TAG" ]; then\n
  echo "usage: dt TAG"\n
else\n
  echo "Tag: $TAG"\n
  TARGET=$(~/.dt/dt_engine $TAG)\n
  echo "Target: $TARGET"\n
  cd "$TARGET"\n
fi\n

