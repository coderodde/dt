
# The directory tagger (dt) stuff.
function dt {
  if [ "$#" -ne 1 ] || [ -z "$1" ];
  then
    echo "usage: dt TAG"
  else
    cd $HOME/.dt
    echo "$1" > "~/.dt/633.directory.tagger.tag.633" # this is so ugly it is unlikely to appear anywhere.
    . ~/.dt/dt_script
  fi
}

